The directory VAO shows you how to define the vertex data in buffers and
vertex array objects (instead of immediate mode in SHADER1). The directory
ROTATE shows you how to use a rotation matrix, defined in the main
program and used in the shader, as a uniform variable.
